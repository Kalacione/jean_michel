# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
wheels/
*.whl
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Virtual environments
.venv/
venv/
env/
ENV/
.env/


# uv
.uv/
uv.lock

# Security — secrets & credentials
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
*.der
secrets/
secrets.toml
secrets.yaml
secrets.json
*_secret*
*_private*
credentials.json
service_account.json
token.json
token.pickle

# Tests
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.idea/
*.swp
*.swo
*~

# polywatch runtime data
data/
alerts/
reports/
*.duckdb
*.duckdb.wal
*.parquet

# Logs
*.log
logs/

# Backups
*.bak
*.backup
*.old
*.vieux
backups/*


# Local overrides
config.local.yaml


# Local datas — runtime SQLite, WAL/SHM sidecars and stray copies
jeanmichel.db
jeanmichel.db-shm
jeanmichel.db-wal
*.db
*.db-shm
*.db-wal
*.db.bak.*
cli_profile.toml
conversations/*
llm_streams/
logs/
# MCP server config (no secrets — auth is via env-var refs — but kept local;
# the curated list ships as mcp_servers.example.toml).
mcp_servers.toml
!mcp_servers.example.toml
# Per-model runtime config (local override ; defaults ship in models.example.toml
# and the built-in table in config.py).
models.toml
!models.example.toml

# Piper voice models (30-150 MB each) — keep only the README tracked
voice_models/*.onnx
voice_models/*.onnx.json
voice_models/*.zip
!voice_models/README.md

# web
node_modules/
**/node_modules/
dist/
**/dist/
build/
**/build/

# code-mode: cached clones of ssh/remote project repos (worktree.py)
repos-cache/
